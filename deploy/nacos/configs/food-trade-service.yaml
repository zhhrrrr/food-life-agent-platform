server:
  port: 8301

spring:
  application:
    name: food-trade-service
  cloud:
    sentinel:
      transport:
        port: ${SENTINEL_TRADE_TRANSPORT_PORT:8721}
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/food_trade_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root

food:
  mq:
    producer-group: food-trade-service-producer
    order-timeout-consumer-group: food-trade-order-timeout-consumer
    order-timeout-delay-level: 16
    retry-delay-seconds: 30
    retry-limit: 50
  jobs:
    trade-event-retry:
      enabled: true
      fixed-delay-ms: 30000
      limit: 50
  trade:
    normal:
      order-timeout-cancel:
        enabled: true
        fixed-delay-ms: 60000
        timeout-minutes: 30
      package-stock-sync:
        enabled: true
        fixed-delay-ms: 60000
    group-buy:
      timeout-compensation:
        enabled: true
        fixed-delay-ms: 60000
    coupon:
      expire-scan:
        enabled: true
        fixed-delay-ms: 60000
        limit: 500
    payment:
      order-timeout-close:
        enabled: true
        fixed-delay-ms: 60000
        timeout-minutes: 30
    seckill:
      order-request-process:
        enabled: true
        fixed-delay-ms: 30000
      order-request-recovery:
        enabled: true
        fixed-delay-ms: 60000
  auth:
    include-paths:
      - /**
    exclude-paths:
      - /health
      - /api/trade/pay/callback/**
